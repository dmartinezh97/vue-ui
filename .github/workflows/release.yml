name: 01 - Crear release

on:
  push:
    tags:
      - 'v*'          # Incluir tags que comiencen con 'v'
      - '!v*-pre'     # Excluir tags que terminen con '-pre'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Extraer código del repositorio
        uses: actions/checkout@v3

      - name: Crear Release
        uses: actions/create-release@v1
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          body: Auto-generated release for ${{ github.ref }}.
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GIT_SECRET }}
